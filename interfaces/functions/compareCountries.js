compareCountries = function(activity, value) {
    var country;
    var offset = 9999;
    dataSet.forEach(function(instance) {
        if(Math.abstimeToMinutes(instance[activity]) - timeToMinutes(value))
    });
} 